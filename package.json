{
  "name": "orrange-website",
  "version": "0.1.0",
  "scripts": {
    "client": "cd web &&HTTPS=true&& yarn start",
    "server": "cd server && nodemon --files server.ts",
    "dev": "HTTPS=true&&concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "concurrently": "^4.1.1",
    "nodemon": "^1.19.1"
  }
}
